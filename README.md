
# Homework_10_best


- В директории src создан модуль processing, который содержит новые функции обработки данных.
- В модуле processing функция filter_by_state, которая принимает список словарей и опционально значение для ключа state. 
- Функция возвращает новый список словарей, содержащий только те словари, у которых ключ state соответствует указанному значению.
- В директории src созданы модули masks, widget. 
в модуле masks заложено две функции: функция маскировки номера банковской карты и функция маскировки номера банковского счета.
В модуле widget создана функция mask_account_card, которая умеет обрабатывать информацию как о картах, так и о счетах.
- В директории src создан модуль generators, который содержит функции для работы с массивами транзакций. 
- В директории src создан модуль decorators, в нем log  применяется к функции my_func_div, которая выполняет деление двух чисел.
При попытке деления на ноль, возникнет ошибка, и в лог будет записано сообщение об этой ошибке и переданных аргументах.
- Для тестирования использован фреймворк pytest.

## ⚙️ Установка
1. Клонируйте репозиторий:
```
https://github.com/Sayyora-code/Homework_10_best.git
```
2. Установите зависимости:
```
pip install -r requirements.txt

3. Установка pytest:
```
pip install pytest. 

4. Команда для запуска тестов:
```
pytest -v

### Требования
- Python 3.9+

## Лицензия:

Этот проект лицензирован по [лицензии MIT](LICENSE). 

